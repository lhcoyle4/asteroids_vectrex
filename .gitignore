# Build artifacts
permadrift.exe
src/*.o
src/*.wasm.o

# WASM outputs — built by GitHub Actions CI, deployed to gh-pages
# Do not commit index.js / index.wasm to master; CI manages them
index.js
index.wasm

# Emscripten / make cruft
*.map
nul

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Game data (per-machine, not repo state)
highscores.dat
